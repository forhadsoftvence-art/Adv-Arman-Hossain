# Adv-Arman-Hossain

অ্যাডভোকেট আরমান হোসেনের (আইনজীবী, বাংলাদেশ সুপ্রিম কোর্ট) পূর্ণাঙ্গ বাংলা ওয়েবসাইট।

## বৈশিষ্ট্য
- সম্পূর্ণ বাংলা ইন্টারফেস (Hind Siliguri + Noto Serif Bengali ফন্ট)
- রেসপনসিভ ডিজাইন — মোবাইল, ট্যাবলেট, ডেস্কটপ
- সেকশন: পরিচিতি, সেবাসমূহ, কর্মপদ্ধতি, মতামত, প্রশ্নোত্তর, যোগাযোগ
- বাংলা সংখ্যায় অ্যানিমেটেড পরিসংখ্যান, স্ক্রল-স্পাই নেভিগেশন, FAQ অ্যাকর্ডিয়ন
- ফর্ম ভ্যালিডেশন (বাংলা ও ইংরেজি দুই ধরনের সংখ্যাসহ) + হোয়াটসঅ্যাপ বাটন
- SEO: Open Graph ট্যাগ ও Schema.org (Attorney) JSON-LD

## চালানোর নিয়ম
যেকোনো স্ট্যাটিক সার্ভারে হোস্ট করা যাবে, যেমন:

```bash
python3 -m http.server 8000
```

তারপর ব্রাউজারে `http://localhost:8000` খুলুন।

## কাস্টমাইজ করতে চাইলে
- **ফোন / হোয়াটসঅ্যাপ নম্বর:** `index.html`-এ `+8801711234567` এবং `wa.me/8801711234567` খুঁজে নিজের নম্বর বসান (বাংলা টেক্সট `+৮৮ ১৭১১-২৩৪৫৬৭`-ও বদলাতে ভুলবেন না)।
- **ইমেইল:** `adv.armanhossain.du@gmail.com` খুঁজে বদলান (`js/main.js`-এও আছে)।
- **ঠিকানা ও সময়সূচি:** `index.html`-এর টপবার ও যোগাযোগ সেকশনে।
- **ছবি:** `assets/img/` ফোল্ডারে।

> নোট: বর্তমান ফোন, ইমেইল ও ঠিকানা নমুনা (placeholder) হিসেবে দেওয়া — প্রকাশের আগে আসল তথ্য বসিয়ে নিন।
